// Thin re-export — dùng Footer chung với role 'student'
import Footer from "@/features/shared/components/Footer";

const StudentFooter = () => <Footer role="student" />;

export default StudentFooter;
